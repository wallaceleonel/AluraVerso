function Caminho(){
var listaFilmes =[]
for (var i = 0; i < listaFilmes.length; i++) {
    document.write("<img src=" + listaFilmes[i] + ">")
    var elementoResultado = document.getElementById("resultado");
    var Caminho = parseInt(document.getElementById("caminhogit").value);
    console.log(Caminho);
    if (listaFilmes != Caminho) {
        //add
        
    } // caminho existente 
}
}